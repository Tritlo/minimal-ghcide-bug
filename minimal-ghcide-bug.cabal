name: minimal-ghcide-bug 
cabal-version: >= 1.24
build-type: Simple
version: 1.0.0

library
  default-language: Haskell2010
  build-depends: base >= 4 && < 5,
                 ghc >= 8 && < 9
  exposed-modules:
    Plugin

